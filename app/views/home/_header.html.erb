<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <%= link_to 'Legal Company', root_path,class:"navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <% unless current_user.admin %>
            <li class="nav-item active">
              <%= link_to "Applications", requests_path, class: "btn btn-light" %>|
            </li>
            <li class="nav-item active">
              <%= link_to "Create application", new_request_path, class: "btn btn-light" %>|
            </li>
          <% end %>

          <li class="nav-item active">
            <%= link_to "Edit profile", edit_user_registration_path, class: "btn btn-light" %>|
          </li>

          <% if current_user.admin %>
            <li class="nav-item active">
              <div class="dropdown">
                <a class="btn btn-light" href="#" role="button" id="dropdownMenuLink"
                   data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Admin panel
                </a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                  <a class="dropdown-item" href="#">
                    <%= link_to "Applications", adminpanel_requests_path %>
                  </a>
                  <a class="dropdown-item" href="#">
                    <%= link_to "Users", adminpanel_users_path %>
                  </a>
                </div>
              </div>
            </li>
            <% end %>

          <li class="nav-item active">
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-light" %>
          </li>
        <% else %>
          <li class="nav-item active">
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-light" %>|
          </li>

          <li class="nav-item active">
            <%= link_to "Sign in", new_user_session_path, class: "btn btn-light" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>